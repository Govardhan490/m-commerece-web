{
  "rules": {
    "buisness": {
      "$uid": {
        ".read": true,
        ".write": "$uid === auth.uid"
      }
    },
    "customer": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
